# 📸 Visual Guide: HTTP Request Nodes Configuration

**Copyright © 2025 Rashad West. All Rights Reserved.**

**Quick Reference:** Step-by-step visual guide for configuring both HTTP Request nodes

---

## 🎯 NODE 1: Vision Analysis (HTTP Request)

### **Step 1: Basic Settings**

```
┌─────────────────────────────────────────┐
│ Vision Analysis (HTTP Request)         │
├─────────────────────────────────────────┤
│ Method: [POST ▼]                       │
│ URL: https://api.openai.com/v1/        │
│        chat/completions                │
│                                         │
│ Authentication:                         │
│ [Credential: OpenAI API Key ▼]         │
└─────────────────────────────────────────┘
```

### **Step 2: Body Configuration**

```
┌─────────────────────────────────────────┐
│ Send Body: [✓]                          │
│ Body Content Type: [JSON ▼]            │
│ Specify Body: [Using JSON ▼]           │
│                                         │
│ JSON Body:                              │
│ ┌─────────────────────────────────────┐│
│ │ {                                    ││
│ │   "model": "gpt-4o",                ││
│ │   "temperature": 0.1,               ││
│ │   "max_tokens": 2000,                ││
│ │   "messages": [...]                  ││
│ │ }                                    ││
│ └─────────────────────────────────────┘│
└─────────────────────────────────────────┘
```

### **Step 3: Headers**

```
┌─────────────────────────────────────────┐
│ Options                                 │
│ ┌─────────────────────────────────────┐│
│ │ Headers                              ││
│ │ ┌─────────────────────────────────┐ ││
│ │ │ Name: Content-Type              │ ││
│ │ │ Value: application/json         │ ││
│ │ └─────────────────────────────────┘ ││
│ └─────────────────────────────────────┘│
└─────────────────────────────────────────┘
```

---

## 🎯 NODE 2: Generate Fix (HTTP Request)

### **Step 1: Basic Settings**

```
┌─────────────────────────────────────────┐
│ Generate Fix (HTTP Request)            │
├─────────────────────────────────────────┤
│ Method: [POST ▼]                       │
│ URL: https://api.openai.com/v1/        │
│        chat/completions                │
│                                         │
│ Authentication:                         │
│ [Credential: OpenAI API Key ▼]         │
└─────────────────────────────────────────┘
```

### **Step 2: Body Configuration**

```
┌─────────────────────────────────────────┐
│ Send Body: [✓]                          │
│ Body Content Type: [JSON ▼]            │
│ Specify Body: [Using JSON ▼]           │
│                                         │
│ JSON Body:                              │
│ ┌─────────────────────────────────────┐│
│ │ {                                    ││
│ │   "model": "gpt-4",                 ││
│ │   "temperature": 0.2,               ││
│ │   "max_tokens": 3000,                ││
│ │   "messages": [...]                  ││
│ │ }                                    ││
│ └─────────────────────────────────────┘│
└─────────────────────────────────────────┘
```

### **Step 3: Headers**

```
┌─────────────────────────────────────────┐
│ Options                                 │
│ ┌─────────────────────────────────────┐│
│ │ Headers                              ││
│ │ ┌─────────────────────────────────┐ ││
│ │ │ Name: Content-Type              │ ││
│ │ │ Value: application/json         │ ││
│ │ └─────────────────────────────────┘ ││
│ └─────────────────────────────────────┘│
└─────────────────────────────────────────┘
```

---

## 🔑 CREDENTIAL SETUP

### **HTTP Header Auth Credential:**

```
┌─────────────────────────────────────────┐
│ Create Credential: HTTP Header Auth      │
├─────────────────────────────────────────┤
│ Name: OpenAI API Key                    │
│                                         │
│ Header Name: Authorization              │
│ Header Value: Bearer sk-proj-...        │
│                                         │
│ [Save]                                  │
└─────────────────────────────────────────┘
```

**Important:** 
- Header Name must be exactly: `Authorization`
- Header Value must start with: `Bearer ` (note the space)
- Then paste your API key: `Bearer sk-proj-abc123...`

---

## 🔗 NODE CONNECTIONS

### **Workflow Flow:**

```
[Webhook] 
    ↓
[Normalize Input]
    ↓
[Prepare Image]
    ↓
[Vision Analysis HTTP] ←─── Configure this
    ↓
[Parse Diagnosis]
    ↓
[Can Auto-Fix?]
    ↓
[Generate Fix HTTP] ←─── Configure this
    ↓
[Parse Fix]
    ↓
[Apply Fix]
```

---

## ✅ VERIFICATION CHECKLIST

### **Vision Analysis Node:**
- [ ] Method = POST
- [ ] URL = `https://api.openai.com/v1/chat/completions`
- [ ] Credential = "OpenAI API Key"
- [ ] Body Content Type = JSON
- [ ] JSON Body contains `"model": "gpt-4o"`
- [ ] JSON Body contains `"image_url"` with `{{ $json.imageUrlForAPI }}`
- [ ] Header: `Content-Type: application/json`

### **Generate Fix Node:**
- [ ] Method = POST
- [ ] URL = `https://api.openai.com/v1/chat/completions`
- [ ] Credential = "OpenAI API Key"
- [ ] Body Content Type = JSON
- [ ] JSON Body contains `"model": "gpt-4"`
- [ ] JSON Body contains `{{ $json.diagnosis.* }}` references
- [ ] Header: `Content-Type: application/json`

---

## 🧪 TEST DATA

### **Test Vision Analysis:**

**Input:**
```json
{
  "context": "Build failed",
  "imageUrlForAPI": "https://via.placeholder.com/800x600.png"
}
```

**Expected Output:**
```json
{
  "choices": [
    {
      "message": {
        "content": "{\"errorType\":\"...\",\"errorMessage\":\"...\"}"
      }
    }
  ]
}
```

### **Test Generate Fix:**

**Input:**
```json
{
  "diagnosis": {
    "errorType": "n8n_workflow",
    "errorMessage": "Could not find property option",
    "affectedSystem": "n8n",
    "likelyCause": "Empty options",
    "filesToFix": ["workflow.json"],
    "severity": "high",
    "canAutoFix": true
  }
}
```

**Expected Output:**
```json
{
  "choices": [
    {
      "message": {
        "content": "{\"fixType\":\"...\",\"filePath\":\"...\",\"fixedCode\":\"...\"}"
      }
    }
  ]
}
```

---

**Quick Setup Time:** 30-45 minutes  
**Status:** Ready to configure  
**Follow:** QUICK-SETUP-HTTP-NODES.md for detailed steps

