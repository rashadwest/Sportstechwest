# One-Command Usage - Everything Flows from a Prompt!

**Yes! Everything flows directly from a single prompt/command.**

---

## 🚀 Super Simple Usage

### Basic Command:
```bash
python3 create_review.py "video.mp4" "What you want to say"
```

**That's it!** The system does everything else automatically.

---

## 📝 Examples

### Example 1: Simple Reaction
```bash
python3 create_review.py "tiktok_video.mp4" "This sports tech is really cool for youth athletes!"
```

**What happens:**
1. ✅ System picks youth character (because you said "youth")
2. ✅ Uses your voice to say the script
3. ✅ Makes the video
4. ✅ Saves it automatically

### Example 2: Robot Content
```bash
python3 create_review.py "tech_video.mp4" "Robots and AI are changing sports analytics forever"
```

**What happens:**
1. ✅ System picks robot character (because you said "robot")
2. ✅ Uses your voice
3. ✅ Makes the video
4. ✅ Done!

### Example 3: Adult Content
```bash
python3 create_review.py "video.mp4" "This is a professional analysis of sports technology"
```

**What happens:**
1. ✅ System picks adult character (default)
2. ✅ Uses your voice
3. ✅ Makes the video
4. ✅ Done!

---

## 🎯 How It Works

### You Type:
```bash
python3 create_review.py "video.mp4" "Your reaction text here"
```

### System Automatically:
1. ✅ Validates video file exists
2. ✅ Creates script from your text
3. ✅ Auto-selects character (youth/robot/adult)
4. ✅ Generates your voice audio
5. ✅ Animates character
6. ✅ Syncs lip movements
7. ✅ Composes final video
8. ✅ Saves output
9. ✅ Tells you where it is

**Zero manual steps after the command!**

---

## ⚙️ Advanced Options

### Specify Character:
```bash
python3 create_review.py "video.mp4" "Your text" --character youth
```

### Custom Output Path:
```bash
python3 create_review.py "video.mp4" "Your text" --output "my_review.mp4"
```

### Verbose Logging:
```bash
python3 create_review.py "video.mp4" "Your text" --verbose
```

### Skip Health Check:
```bash
python3 create_review.py "video.mp4" "Your text" --skip-health-check
```

---

## 🔄 Complete Flow

```
Your Command
    ↓
System Validates Everything
    ↓
Auto-Selects Character
    ↓
Generates Voice Audio
    ↓
Animates Character
    ↓
Syncs Lip Movements
    ↓
Composes Video
    ↓
Saves Output
    ↓
Done! 🎉
```

**All from one command!**

---

## 📋 What You Need First

**Before you can use one command, you need to:**
1. ✅ Get API key (5 min)
2. ✅ Record voice (10 min)
3. ✅ Get characters (1-2 hrs)
4. ✅ Run setup wizard (5 min)

**After that, it's just one command forever!**

---

## 🎯 Future: Even Simpler

### Coming Soon:
```bash
# Just give it a TikTok URL and your thoughts
python3 create_review.py "https://tiktok.com/@user/video/123" "This is cool!"
```

**System will:**
- ✅ Download video automatically
- ✅ Everything else as before

---

## 💡 Pro Tips

1. **Write natural prompts** - The system auto-selects character based on keywords
2. **Use keywords** - Say "youth" or "robot" to trigger character selection
3. **Keep it simple** - Just say what you want to say, system handles the rest

---

## ✅ Summary

**Yes! Everything flows from one prompt:**

```bash
python3 create_review.py "video.mp4" "Your reaction text"
```

**That's all you need!** 🚀

