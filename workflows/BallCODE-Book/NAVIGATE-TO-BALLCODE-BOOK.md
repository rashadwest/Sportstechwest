# How to Navigate to BallCODE-Book

**Date:** December 18, 2025  
**Purpose:** Quick reference for accessing the BallCODE-Book directory

---

## 🎯 QUICK COMMAND

**From anywhere:**
```bash
cd /Users/rashadwest/Sportstechwest/workflows/BallCODE-Book
```

**Or use the alias (add to your ~/.zshrc):**
```bash
alias ballcode='cd /Users/rashadwest/Sportstechwest/workflows/BallCODE-Book'
```

Then just type: `ballcode`

---

## 📍 FULL PATH

```
/Users/rashadwest/Sportstechwest/workflows/BallCODE-Book
```

---

## 🔍 VERIFY YOU'RE IN THE RIGHT PLACE

**Check current directory:**
```bash
pwd
```

**Should show:**
```
/Users/rashadwest/Sportstechwest/workflows/BallCODE-Book
```

**List files to confirm:**
```bash
ls -la
```

**Should see files like:**
- `UNITY-LANDING-PAGE-UI-IMPROVEMENT-GUIDE.md`
- `scripts/` directory
- `n8n-*.json` files
- etc.

---

## 🚀 QUICK ACCESS FROM TERMINAL

**Option 1: Full path**
```bash
cd /Users/rashadwest/Sportstechwest/workflows/BallCODE-Book
```

**Option 2: Relative from home**
```bash
cd ~/Sportstechwest/workflows/BallCODE-Book
```

**Option 3: From workflows directory**
```bash
cd ~/Sportstechwest/workflows
cd BallCODE-Book
```

---

## 📝 ADD ALIAS (Recommended)

**Add to your shell config:**
```bash
# Add to ~/.zshrc (or ~/.bashrc)
echo 'alias ballcode="cd /Users/rashadwest/Sportstechwest/workflows/BallCODE-Book"' >> ~/.zshrc
source ~/.zshrc
```

**Then use:**
```bash
ballcode
```

---

**You're now in the BallCODE-Book directory!** ✅

